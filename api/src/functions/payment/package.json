{
  "name": "payment-webhook",
  "version": "0.0.1",
  "description": "When paystack payments are made, this function is called to update the database",
  "main": "scheduled-data-aggregation.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "firebase-admin": "^11.8.0",
    "neo4j-driver": "^5.0.1"
  }
}
